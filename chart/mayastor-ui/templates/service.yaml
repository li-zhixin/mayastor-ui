apiVersion: v1
kind: Service
metadata:
  name: {{ include "mayastor-ui.fullname" . }}
  labels:
    {{- include "mayastor-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mayastor-ui.selectorLabels" . | nindent 4 }}
